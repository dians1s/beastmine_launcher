# BeastMine Launcher v2.0

Максимально производительный Minecraft лаунчер на Rust + Tauri + React

## ⚡ Особенности

- **Максимальная производительность**: Написан на Rust с оптимизациями LTO
- **Красивый UI**: Glassmorphism дизайн с Framer Motion анимациями
- **Мгновенный запуск**: Оптимизированные JVM аргументы для Minecraft
- **Система обновлений**: Автоматические обновления через Tauri
- **Модпаки**: Поддержка модифицированных версий
- **Управление скинами**: Загрузка и смена скинов

## 🚀 Быстрый старт

### Windows

#### Вариант 1: С MinGW (рекомендуется)

```batch
setup-mingw.bat
:: Перезапустите терминал
cd beastmine-launcher
cargo build --release
```

#### Вариант 2: С Visual Studio

```powershell
setup-windows.ps1
:: Перезапустите терминал
cd beastmine-launcher
cargo build --release
```

### Linux/macOS

```bash
cd beastmine-launcher
cargo build --release
```

## 🛠️ Разработка

```bash
# Frontend
cd frontend
npm install
npm run dev

# Backend (в другом терминале)
cd ..
cargo tauri dev
```

## 📦 Структура проекта

```
beastmine-launcher/
├── src-tauri/          # Rust бэкенд
│   ├── src/
│   │   ├── commands.rs    # Tauri команды
│   │   ├── models.rs      # Модели данных
│   │   └── services/      # Сервисы
│   ├── Cargo.toml
│   └── tauri.conf.json
├── frontend/           # React фронтенд
│   ├── src/
│   │   ├── components/    # React компоненты
│   │   ├── pages/         # Страницы
│   │   └── store/         # Zustand store
│   └── package.json
└── README.md
```

## 🎨 Технологии

**Backend:**
- Rust + Tokio (асинхронность)
- Tauri v2 (desktop framework)
- Reqwest (HTTP клиент)
- Serde (сериализация)

**Frontend:**
- React 18 + TypeScript
- Vite (сборка)
- Tailwind CSS (стили)
- Framer Motion (анимации)
- Zustand (state management)

## ⚙️ Оптимизации

### Rust
- `opt-level = 3` - максимальная оптимизация
- `lto = true` - Link Time Optimization
- `strip = true` - удаление символов
- `codegen-units = 1` - лучшая оптимизация
- `panic = "abort"` - меньший размер бинарника

### Minecraft JVM
- G1GC для лучшей производительности
- Оптимизированные флаги памяти
- Parallel reference processing
- String deduplication

## 📄 Лицензия

MIT License
